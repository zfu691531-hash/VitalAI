# 新版校规问答第一期准备说明

## 当前状态
- 旧版校规问答仍然保留，可继续使用
- 新版校规问答试运行页已经接入
- 新增了 `rule-rag`、`rule-feedback`、`rule-kb` 三组接口
- MySQL 新增问答记录、反馈、切片、同步任务四类数据表
- 当前新版链路已可运行，但 Milvus 仍处于“可接入、未正式启用”的准备状态

## 已完成的准备项
- 在 [backend/requirements.txt](/d:/Users/Windows/PycharmProjects/aistu/backend/requirements.txt) 中补充 `pymilvus`
- 在 [backend/.env.example](/d:/Users/Windows/PycharmProjects/aistu/backend/.env.example) 中补充：
  - `AI_EMBEDDING_MODEL_NAME`
  - `MILVUS_URI`
  - `MILVUS_TOKEN`
  - `MILVUS_COLLECTION`
- 新增手动重建脚本 [rebuild_rule_kb.py](/d:/Users/Windows/PycharmProjects/aistu/backend/scripts/rebuild_rule_kb.py)

## 下一步准备清单
1. 安装后端新增依赖
   在 `backend` 目录执行：
   `pip install -r requirements.txt`

2. 准备 Milvus 服务
   至少需要一套可用的：
   - 本地 Milvus
   - Docker Milvus
   - 或远程 Milvus / Zilliz Cloud

3. 配置环境变量
   在 `backend/.env` 中补齐：
   - `AI_EMBEDDING_MODEL_NAME`
   - `MILVUS_URI`
   - `MILVUS_TOKEN`
   - `MILVUS_COLLECTION`

4. 执行知识库重建
   在项目根目录执行：
   `python backend/scripts/rebuild_rule_kb.py`

5. 验证新版试运行链路
   - 新版校规问答是否能返回回答
   - 命中来源是否显示
   - 反馈是否能写入反馈中心
   - 管理员是否能采纳/驳回反馈

## 当前限制
- 目前新版链路的混合检索已可用，但稠密向量仍是本地轻量实现
- 在 Milvus 和真实 embedding 模型接通前，新版问答更适合作为“试运行版本”
- 旧版功能不会受影响，可继续兜底
