6.1 验证流程七步法
步骤	名称	检查什么	如何检查
1	功能验证	代码是否完成了任务？	直接运行，测试基本场景
2	上下文验证	是否符合你的项目架构？	和现有代码对比，接口是否一致
3	质量评估	可读性、可维护性如何？	代码是否有注释、变量名是否清晰
4	依赖审查	引入的库是否真实存在？	pip install 试试，看是否报错
5	安全检查	有没有SQL注入、明文密码等问题？	对照安全检查清单逐项检查
6	性能测试	高负载下是否稳定？	简单压测，或让AI分析时间复杂度
7	人工审查	业务逻辑是否正确？	这是最重要的，AI不懂你的业务
6.2 AI幻觉识别清单（高发场景）
⚠️  AI幻觉是指AI生成了「看起来合理但实际错误」的内容。代码幻觉最难发现，因为代码语法正确，但调用了不存在的函数或错误的参数。

幻觉类型	典型表现	检测方法
API幻觉	调用了不存在的方法（如 .save_to_db()）	运行代码，看 AttributeError
库幻觉	导入了不存在的包（如 from langchain import xxx）	pip install 后 python -c "import xxx" 测试
版本幻觉	用了老版本的API（LangChain 0.1 vs 0.3 API差别很大）	查官方文档对应版本，@web 搜索最新用法
参数幻觉	函数参数名或类型写错	看函数签名，运行时会 TypeError
逻辑幻觉	代码语法正确但逻辑错误	边界条件测试：空输入、超大值、特殊字符

快速检测脚本（让AI帮你写）
🤖 向AI发送：「帮我写一个 Python 脚本，检查以下文件中所有的 import 语句，验证每个被导入的包是否真实存在（用 importlib 尝试导入）。文件路径：[你的py文件路径]。对每个包输出：✓ 存在 或 ✗ 不存在（可能幻觉）。」

6.3 安全检查要点
检查项	危险信号（AI容易生成）	正确做法
SQL查询	f"SELECT * FROM users WHERE id={user_id}"	使用参数化查询：execute(sql, (user_id,))
密码存储	直接存明文或用MD5	使用 bcrypt 或 argon2 哈希
用户输入	直接使用 request.get('input') 构造命令	用 Pydantic 验证类型和格式
API密钥	api_key = 'sk-xxx' 硬编码	用 os.getenv('API_KEY') 从环境变量读
文件上传	直接使用用户提供的文件名	用 uuid 重命名，限制扩展名和大小
错误信息	except: return str(e)（暴露内部信息）	记录日志，返回通用错误消息

6.4 代码质量检查表
•□ 每个函数不超过50行（超了就让AI拆分）
•□ 函数名和变量名清晰（看名字就知道是干什么的）
•□ 有 docstring 说明函数的输入输出
•□ 有异常处理（try-except），不让程序崩溃
•□ 没有重复代码（DRY原则：Don't Repeat Yourself）
•□ 类型注解完整（def func(name: str) -> dict:）
•□ 单元测试覆盖核心逻辑
•□ 没有 print 调试代码遗留在生产代码里