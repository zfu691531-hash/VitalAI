校园AI教务助手平台 产品整合版·前后端开发需求文档
**产品定位**：面向中小学教育场景，打造一站式教务管理与AI智能辅助一体化平台，深度整合**学生信息管理系统（核心增删改查全流程）** 与 **8大AI教务智能工具**，严格区分**学生、教师、校务管理员**三级角色权限，全面覆盖教务日常管理、教学辅助支撑、行政办公高效处理等全场景，助力校园教务工作提质增效、简化流程。
**核心模块**：基础权限管控体系 → 教务核心数据CRUD全操作 → AI智能教务工具矩阵 → 数据统计与多格式导出功能，各模块无缝衔接、协同运转。
**整合说明**：将原有8大AI功能模块、两级分班分组逻辑（教师分组、校务分班），以及学生、教师、班级、成绩的全生命周期管理功能进行深度融合优化，剔除功能冗余项，打通各业务环节的数据壁垒，形成“数据录入-管理操作-智能辅助-统计导出”的完整业务闭环，确保操作流程连贯、数据互通共享。

一、前端开发要求
1. 技术选型要求
结合校园内网使用场景的特殊性，优先选择轻量化、高性能、易开发的技术栈，具体要求如下：
1.框架：**Vue3 + Vite**，兼顾轻量化与开发效率，适配校园内网带宽有限、硬件配置多样的使用环境，确保页面加载流畅无卡顿；
2.UI组件库：采用Element Plus，贴合管理系统的操作习惯，组件丰富、易用性强，可大幅提升前端开发效率，保证界面风格统一；
3.状态管理：使用Pinia，高效存储用户身份信息、角色权限配置、AI生成历史记录等核心数据，实现跨组件数据共享与状态同步；
4.网络请求：基于Axios进行统一封装，实现请求拦截、响应拦截、权限校验、异常处理等功能，确保前后端数据交互安全、稳定；
5.导出功能：支持Excel、Word、图片三种格式导出，分别基于xlsx插件、html-to-image插件实现，满足教务工作中不同场景的导出需求；
6.适配要求：以PC端（1920*1080分辨率）为主要适配场景，同时兼容平板设备操作，确保教师、管理员在不同终端均可便捷使用。
2. 页面模块与功能要求（全整合版）
（1）通用基础模块
作为平台的基础支撑，覆盖所有角色的通用操作场景，确保使用便捷性与安全性：
•登录/注册页：支持角色切换选择（学生/教师/校务管理员），采用账号密码登录模式，搭配密码可见/隐藏切换功能，登录失败给出明确错误提示（如账号密码错误、权限异常）；
•首页/控制台：展示核心数据概览，包括班级总人数、各班级学生分布、近期考试成绩统计、AI工具快捷入口（按使用频率排序），支持快速跳转至常用功能模块，提升操作效率；
•个人中心：支持个人基础信息（姓名、联系方式等）修改、登录密码重置，同时展示个人AI生成历史记录，支持记录查看、内容复用、删除操作，方便后续快速调用；
•权限拦截：实现页面级与按钮级双重权限控制，无对应权限的用户访问相关页面时自动跳转至登录页或无权限提示页，例如校务管理员专属的分班功能，教师与学生无法可见、无法操作。
（2）核心教务管理系统（增删改查全覆盖，适配三级权限）
模块	前端功能要求
学生信息管理	展示学生列表（支持分页、关键词搜索、多条件筛选）；支持单个新增/编辑/删除学生信息，信息字段包括姓名、性别、年龄、所属班级（填写班级ID关联班级）、联系方式、个人特长、违纪标签、关怀标签（如留守儿童、学困生）；支持学生信息批量导入（提供标准Excel模板下载）、批量导出，导入时校验数据合法性（含班级ID有效性）并提示错误信息。
教师信息管理	展示全校教师列表（支持按学科、职务、绑定班级筛选）；支持单个新增/编辑/删除教师信息，信息字段包括姓名、任教科目、职务（班主任/普通教师）、绑定班级（填写班级ID，可多个，用逗号分隔）；支持为教师分配角色权限，明确教师可操作的功能范围，确保权限精准管控。
班级管理	展示全年级班级列表（支持按年级筛选）；支持单个新增/编辑/删除班级，信息字段包括年级、班级名称、班主任（填写教师ID关联教师）、人数上限；支持班级与学生的关联操作（通过班级ID与学生ID关联），可批量添加/移除班级学生，实时同步班级人数统计。
成绩管理	支持成绩单个录入/编辑/删除，绑定学生（学生ID）、班级（班级ID）、考试批次（如期中、期末）、各科分数等信息；支持成绩批量导入（适配Excel模板），导入后可批量修改、批量删除；以表格形式展示成绩数据，支持按学生姓名、班级、考试批次、科目筛选查询，直观呈现学生成绩情况。
（3）AI智能教务工具模块（优化整合版，贴合校园实际办公需求）
8大AI工具均优化交互流程，简化操作步骤，生成内容可直接复用、导出，适配教师日常教务工作场景，具体要求如下：
1.**高情商期末评语生成器**
2.输入区域：支持手动输入学生关键词（如“勤奋好学、乐于助人”），下拉选择学生年级、性别，提升评语针对性；
3.操作功能：设置生成按钮，生成后支持一键复制、导出（Word格式），自动保存生成记录至个人历史，便于后续修改复用；
4.批量功能：支持绑定指定班级（通过班级ID），批量为班级内所有学生生成评语，生成后可逐一生成、批量导出，节省教师工作量。
5.**违纪处理话术教练**
6.输入区域：手动输入具体违纪场景（如“上课迟到、课堂打闹”），下拉选择沟通模式（温和/严厉）、沟通对象（学生/家长）；
7.输出功能：自动生成三段式标准话术（开场引导、问题说明、引导改进），话术贴合沟通对象与模式，支持一键复制，可直接用于沟通。
8.**班级公告润色助手**
9.输入区域：手动输入公告草稿，下拉选择润色风格（正式/幽默/亲切）、使用场景（班级群发布/校园公示栏）；
10.输出功能：自动生成带规范标题、落款（发布人、发布时间）的标准通知，排版工整，可直接复制发布或导出使用。
11.**校规问答机器人**
12.交互功能：支持用户手动输入校规相关问题（如“迟到一次如何处理”“请假流程是什么”）；
13.输出功能：AI精准匹配校规内容，给出清晰、简洁的回答，支持追问；校规文本可由校务管理员在后台更新，确保问答内容的时效性。
14.**成绩波动诊断书**
15.操作功能：支持手动选择学生（通过学生ID），系统自动读取数据库中学生的历史成绩数据（通过学生ID关联），无需手动输入；
16.输出功能：一键生成成绩波动诊断报告，包含成绩变化趋势、波动原因分析、改进建议，支持导出（Word/Excel）、分享给学生或家长。
17.**班会活动策划师**
18.输入区域：手动输入班会主题（如“感恩成长”“学风建设”），下拉选择年级、班会时长（如45分钟、90分钟）；
19.输出功能：生成完整的班会教案，包含活动目标、流程步骤、互动环节、物料清单，可直接用于班会开展，支持导出、修改复用。
20.**模拟面试官**
21.输入区域：手动输入学生简介（如年级、特长、薄弱环节）；
22.交互功能：支持多轮对话交互，AI根据学生简介提出针对性问题，学生回答后，AI给出专业点评与改进建议，模拟真实面试场景。
23.**智能分组/分班助手（两级差异化，适配不同角色）**
24.教师端（班级内分组）：仅可操作本班学生（通过教师绑定的班级ID关联），用于学习小组、活动小组分组，支持自定义分组人数、分组规则（如学习成绩互补、性格互补），生成小组列表，可导出、修改；
25.校务端（全年级分班）：可操作全年级新生（通过年级关联班级ID、学生ID），用于正式分班，强制遵循均衡规则（成绩均衡、男女比例均衡、关怀生合理分配），生成分班表格，明确各班级学生名单，支持导出、打印，作为分班依据。
3. 交互与体验要求
围绕校园用户使用习惯，优化交互流程，提升操作便捷性与体验感，具体要求如下：
1.增删改查操作：所有核心数据的新增、删除、修改操作，均弹出二次确认弹窗，明确提示操作内容（如“确认删除该学生信息？删除后不可恢复”），防止误操作；
2.AI生成功能：点击生成按钮后，显示加载动画，提示“正在生成，请稍候”；若生成失败，给出明确错误提示，并提供重试按钮；生成成功后，支持一键复制，操作便捷；
3.批量操作：所有支持批量处理的模块（学生导入、评语生成、成绩修改等），均支持批量选择、批量导入导出、批量生成，减少重复操作，提升工作效率；
4.数据渲染：表格数据支持分页展示（可自定义每页条数），提供关键词搜索、多条件筛选功能，快速定位所需数据（如学生、班级、成绩）；
5.响应反馈：所有操作（登录、新增、修改、导出等）均给出明确的成功/失败提示，表单提交时进行必填项校验，未填写必填项时提示用户补充，避免无效提交。
4. 视觉与格式要求
1.整体风格：简洁、正式、清爽，贴合校园办公场景，避免花哨设计，颜色以浅色系为主，重点内容（如操作按钮、关键数据）适当突出，提升可读性；
2.AI工具输出内容：排版美观、分段清晰、字体规范，无杂乱格式，生成的评语、教案、诊断书等内容可直接打印使用，无需二次排版；
3.管理系统表格：表格字段对齐整齐，操作按钮（如编辑、删除、导出）样式统一、位置一致，关键数据（如不及格成绩、关怀生标签）进行高亮标注，便于快速识别。

二、后端开发要求
1. 技术选型要求
结合平台高性能、轻量化、可部署性的需求，适配校园内网环境，技术选型如下：
1.框架：采用**FastAPI**，具备高性能、异步支持、自动生成接口文档等优势，适配AI接口调用的高并发场景，同时开发效率高，便于后期维护；
2.数据库：使用MySQL 8.0，稳定可靠、兼容性强，用于存储学生、教师、班级、成绩、用户信息等核心数据，支持海量数据的高效查询与存储；
3.ORM：采用SQLAlchemy，简化数据库操作，实现数据访问层与业务逻辑层的解耦，提升代码可读性与可维护性；
4.AI服务：对接大模型API，统一封装AI调用接口，实现接口标准化，便于前端调用，同时支持后续更换大模型时的快速适配；
5.文件处理：支持Excel、Word格式的导出与批量导入解析，提供标准模板下载，实现数据的快速导入与导出，适配教务工作需求；
6.权限控制：采用JWT token认证机制，实现用户登录校验、角色权限校验，确保接口访问安全，防止越权操作；
7.部署要求：轻量化部署，支持校园内网部署，无需复杂的服务器配置，确保平台在校园内网环境下稳定运行，无额外依赖。
2. 核心架构与规范
1.前后端分离：严格遵循**RESTful API**设计规范，接口命名统一、语义清晰，便于前端对接开发，同时支持接口的复用与扩展；
2.三层架构：采用路由层→业务逻辑层→数据访问层的三层架构，职责分工明确：
3.路由层：负责接收前端请求，转发至对应业务逻辑模块，返回处理结果；
4.业务逻辑层：负责处理核心业务逻辑，实现权限校验、数据处理、AI接口调用等功能，通过字段关联（ID匹配）实现原外键关联的业务逻辑；
5.数据访问层：负责与数据库交互，实现数据的增删改查操作，屏蔽数据库底层细节，不设置外键约束，仅通过对应ID字段实现数据关联查询。
6.统一响应格式：所有接口返回数据均采用`code/msg/data`的统一格式，其中code为状态码（成功200、失败非200），msg为提示信息，data为返回的核心数据，便于前端统一解析处理；
7.异常处理：实现全局异常捕获机制，对接口调用过程中出现的异常（如数据库错误、AI调用失败、权限异常、关联ID不存在）进行统一处理，返回友好的错误信息，便于问题排查与定位。
3. 核心接口开发要求
（1）权限与用户接口
围绕用户登录、权限校验、个人信息管理，开发以下核心接口，确保用户访问安全：
•登录接口：接收前端传入的账号、密码、角色信息，验证账号密码的合法性，验证通过后返回JWT token与用户角色信息，供前端存储与后续接口调用；
•权限校验接口：对所有需要权限的接口进行拦截校验，验证用户token的有效性与角色权限，非授权访问时返回权限异常提示，拦截接口调用；
•个人信息接口：支持查询当前登录用户的个人基础信息，支持修改个人信息（不含角色、账号）、重置登录密码，修改后同步更新数据库数据。
（2）教务管理CRUD接口（核心必做，适配三级权限）
覆盖学生、教师、班级、成绩四大核心模块，实现全流程CRUD操作，接口需适配不同角色的权限范围，通过ID字段关联实现数据联动，具体要求如下：
1.**学生管理接口**：
2.查询接口：支持分页查询、关键词搜索、多条件筛选，根据用户角色返回对应范围的数据（学生仅查个人，教师仅查本班（通过班级ID关联），校务查全量）；
3.操作接口：新增学生、编辑学生、删除学生（单个/批量），支持学生信息批量导入（校验数据合法性，含班级ID有效性）、批量导出（按筛选条件导出）；删除学生时，同步校验关联数据（如成绩、AI历史记录），确保数据一致性。
4.**教师管理接口**：
5.查询接口：分页查询、多条件筛选（学科、职务、班级），校务可查全量教师，教师仅可查自身信息；查询时可通过绑定的班级ID关联查询对应班级信息；
6.操作接口：新增、编辑、删除教师信息，支持教师与班级的绑定/解绑操作（通过班级ID关联），支持为教师分配角色权限；删除教师时，同步校验关联数据（如班级班主任绑定），确保数据一致性。
7.**班级管理接口**：
8.查询接口：分页查询、按年级筛选，校务可查全量班级，教师仅可查自身绑定的班级（通过班级ID关联）；查询时可通过班主任ID关联查询对应教师信息；
9.操作接口：新增、编辑、删除班级信息，支持班级与学生的关联查询、批量关联/解绑操作（通过班级ID与学生ID关联），实时更新班级人数统计；删除班级时，同步校验关联数据（如学生、成绩），确保数据一致性。
10.**成绩管理接口**：
11.查询接口：按学生（学生ID）、班级（班级ID）、考试批次、科目筛选查询成绩，支持成绩统计（平均分、最高分、最低分），学生仅查个人成绩，教师仅查本班成绩（通过班级ID关联），校务查全量成绩；
12.操作接口：成绩录入、编辑、删除（单个/批量），支持成绩批量导入，导入时校验数据合法性（如学生ID、班级ID是否存在、科目是否合理）。
（3）AI智能工具统一接口
将8大AI功能封装为独立接口，接口参数标准化、统一化，便于前端调用与后期维护，通过ID字段关联数据库数据，具体要求如下：
•接口封装：每个AI功能对应一个独立接口，接收前端传入的参数，调用统一封装的大模型API，处理生成结果后返回给前端；
•参数标准化：各接口参数明确、统一，具体参数如下：
•评语生成接口：`keywords`（学生关键词）、`grade`（年级）、`gender`（性别），可传入学生ID关联学生信息；
•违纪话术接口：`incident`（违纪场景）、`mode`（沟通模式）、`target`（沟通对象）；
•通知润色接口：`draft`（公告草稿）、`style`（润色风格）、`scene`（使用场景），可传入班级ID关联班级信息；
•校规问答接口：`question`（用户问题）；
•成绩诊断接口：`student_id`（学生ID），通过ID查询数据库中学生的历史成绩数据，实现数据联动；
•班会策划接口：`theme`（班会主题）、`grade`（年级）、`time`（班会时长），可传入班级ID关联班级信息；
•模拟面试接口：`profile`（学生简介）、`chat_history`（对话历史），可传入学生ID关联学生信息；
•分组/分班接口：`role`（用户角色，teacher/admin）、`student_ids`（学生ID列表）、`rules`（分组/分班规则），教师端需传入自身绑定的班级ID，校务端可传入年级关联班级ID。
（4）历史记录接口
用于记录用户AI生成操作的历史数据，支持查看、复用、删除，通过ID字段关联用户信息，具体接口如下：
•保存接口：用户使用AI工具生成内容后，自动保存生成记录（关联用户ID、功能类型、生成内容、创建时间），可关联学生ID、班级ID便于后续查询；
•查询接口：根据用户ID查询个人AI生成历史记录，支持分页、按功能类型筛选，可通过学生ID、班级ID筛选对应记录；
•复用接口：支持根据历史记录ID，调取历史生成内容，供用户修改、复用；
•删除接口：支持单个/批量删除个人AI生成历史记录，删除后不可恢复。
4. 数据库设计要求（核心表，字段可根据实际需求补充，无外键）
数据库设计需遵循规范化原则，确保数据完整性、一致性，核心表设计如下，各表通过对应ID字段实现关联，不设置外键约束，具体如下：
1.**用户表(user)**：存储所有用户的登录信息与基础身份信息，字段包括id（主键）、账号（唯一）、密码（加密存储）、角色(student/teacher/admin)、姓名、创建时间、更新时间；
2.**学生表(student)**：存储学生详细信息，字段包括id（主键）、姓名、性别、班级id（用于关联班级表，不设外键）、联系方式、特长、标签（违纪/关怀，用逗号分隔）、创建时间、更新时间；
3.**教师表(teacher)**：存储教师详细信息，字段包括id（主键）、姓名、学科、职务、绑定班级id（用于关联班级表，可多个班级，用逗号分隔，不设外键）、创建时间、更新时间；
4.**班级表(class)**：存储班级信息，字段包括id（主键）、年级、班级名（唯一）、班主任id（用于关联教师表，不设外键）、人数上限、当前人数、创建时间、更新时间；
5.**成绩表(score)**：存储学生成绩信息，字段包括id（主键）、学生id（用于关联学生表，不设外键）、班级id（用于关联班级表，不设外键）、考试批次、科目、分数、创建时间、更新时间；
6.**AI历史表(ai_history)**：存储AI生成历史记录，字段包括id（主键）、用户id（用于关联用户表，不设外键）、功能类型（如评语生成、违纪话术）、生成内容、创建时间、更新时间，可新增学生id、班级id字段用于关联查询。
5. 业务逻辑与权限要求
严格遵循三级角色权限隔离原则，明确各角色的操作范围，通过ID字段关联实现数据联动，确保业务逻辑合理、数据安全，具体要求如下：
1.**三级权限严格隔离**
2.学生角色：仅可查询个人基础信息、个人成绩（通过自身ID关联），使用校规问答、模拟面试两种AI工具，无法进行任何管理操作；
3.教师角色：可管理本班学生信息、本班学生成绩（通过自身绑定的班级ID关联），使用全部8大AI工具，仅可进行本班学生的分组操作（通过班级ID关联学生），无法查看、操作其他班级数据；
4.校务管理员角色：可管理全校所有学生、教师、班级、成绩数据，使用全部8大AI工具，可进行全年级新生分班（通过年级关联班级ID、学生ID）、系统配置、校规内容更新等操作，拥有最高权限。
5.**分班/分组业务规则**
6.教师端分组：仅可操作本班学生（通过教师绑定的班级ID关联学生ID），分组规则灵活，支持自定义人数、互补规则，分组结果仅用于本班内部使用，不影响正式分班；
7.校务端分班：仅可操作全年级新生（通过年级关联班级ID、学生ID），强制遵循均衡规则，确保各班级成绩均衡、男女比例均衡、关怀生合理分配，分班结果为正式分班依据，不可随意修改。
8.**数据安全**：用户密码采用加密算法存储（如MD5加密），防止密码泄露；所有接口均需进行JWT token校验，防止未授权访问；严格控制数据访问权限，通过ID字段关联校验用户可操作的数据范围，避免越权查询、操作数据，确保数据安全；删除数据时，需校验关联数据，避免出现孤立数据。
6. 通用能力要求
1.数据校验：实现前端+后端双重校验，前端校验必填项、数据格式，后端再次校验数据合法性（如手机号格式、分数范围、关联ID有效性），避免无效数据录入数据库；
2.批量导入导出：支持Excel模板下载，用户可按照模板填写数据后批量导入，导入时校验数据合法性（含关联ID有效性），给出明确的错误提示（如“学生ID不存在”“分数格式错误”“班级ID无效”）；批量导出支持按筛选条件导出，确保导出数据准确、格式规范；
3.AI服务容错：AI接口调用时实现超时重试机制（最多3次），若多次调用失败，进行降级处理，返回友好提示（如“当前AI服务繁忙，请稍后再试”），避免系统崩溃；
4.日志记录：建立完善的日志记录机制，记录用户操作日志（如登录、新增、删除）、AI调用日志（调用时间、功能类型、参数、结果）、异常日志（异常类型、发生时间、错误信息，含关联ID异常），便于后期问题排查与系统优化。

三、前后端联调核心要求
为确保前后端开发同步、功能正常衔接，联调工作需遵循以下核心要求，重点校验ID关联逻辑，确保平台整体功能可用、稳定：
1.接口文档统一：后端基于FastAPI自动生成接口文档，明确接口地址、请求参数、响应格式、权限要求，标注各接口所需关联ID字段，前端对照接口文档进行开发，避免接口对接偏差；
2.AI功能联调：优先完成教务管理核心CRUD接口的联调，重点校验ID关联逻辑（如学生ID与班级ID匹配、教师ID与班级ID匹配），确保数据交互正常后，再进行AI智能工具接口的联调，重点测试AI生成功能的稳定性、数据准确性；
3.权限联调：严格测试不同角色的功能可见性与操作权限，校验角色与关联ID的数据访问范围（如教师仅能访问自身绑定班级ID的数据），确保学生、教师、校务管理员的权限边界清晰，无越权访问、功能错乱等问题；
4.导出功能联调：测试Excel、Word、图片三种格式的导出功能，确保导出文件格式无误、数据完整（含关联ID对应的名称展示），无乱码、缺失等问题，适配不同办公软件打开；
5.批量操作联调：重点测试批量导入、批量生成、批量删除等功能，验证数据处理的准确性、效率，重点校验批量操作中关联ID的合法性，确保批量操作无数据丢失、错误、孤立数据等问题。

四、核心业务规则（必遵守）
为确保平台功能贴合校园教务实际需求、业务流程规范，所有开发工作需严格遵守以下核心业务规则，不得随意修改，重点保障ID关联逻辑正常：
1.**分班功能不重复**：教师端仅支持本班学生的临时分组（用于学习、活动），通过教师绑定的班级ID关联学生；校务端仅支持全年级新生的正式分班，通过年级关联班级ID、学生ID，两套功能逻辑独立，互不干扰，避免功能冗余与操作混乱；
2.**AI功能闭环**：所有AI工具生成的内容（评语、话术、教案等），均需支持保存至历史记录、后续复用、多格式导出，贴合教师日常办公习惯，减少重复工作量；生成内容需关联对应学生ID、班级ID，便于后续查询复用；
3.**数据互通**：成绩波动诊断、智能分组/分班、期末评语生成等功能，需通过学生ID、班级ID直接读取数据库中的学生信息、成绩数据，无需用户手动输入，确保数据一致性、准确性，提升操作效率；
4.**轻量化优先**：平台开发需遵循轻量化原则，剔除冗余功能与不必要的交互，优化代码性能，适配中小学校园的硬件配置与内网环境，确保平台运行流畅、无卡顿，降低部署与维护成本；同时优化ID关联查询逻辑，提升数据查询效率。
|（注：文档部分内容可能由 AI 生成)